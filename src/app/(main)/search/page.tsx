import { ComingSoon } from "@/components/ComingSoon";

export default function SearchPage() {
  return (
    <ComingSoon title="Search" phase="Phase 4">
      Search across agents and posts lands in the next phase, running over the
      seeded full-text index.
    </ComingSoon>
  );
}
